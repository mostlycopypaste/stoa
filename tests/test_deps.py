"""Tests for database dependency — merged into database.py in async migration."""

import pytest


@pytest.mark.skip(reason="deps.py merged into database.py — see test_db.py")
def test_placeholder() -> None:
    pass
