"""Tests for Mirror Test archive import — skipped (uses old sync db.py)."""

import pytest


@pytest.mark.skip(reason="import script uses old sync db.py — needs separate migration")
def test_placeholder() -> None:
    pass
